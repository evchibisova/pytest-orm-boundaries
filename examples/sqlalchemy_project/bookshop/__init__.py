"""A tiny SQLAlchemy bookshop, mirroring the Django sample_project one-to-one.

The point is parity: the same aggregates and the same boundary crossings, but
expressed through SQLAlchemy Core/ORM instead of Django's ORM.
"""
